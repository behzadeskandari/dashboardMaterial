import React from "react";

export const ArabicReducer = (state = 0, action) => state;

export const PersianReducer = (state = 1, action) => state;

export default EnglishReducer = (state = 2, action) => state;
